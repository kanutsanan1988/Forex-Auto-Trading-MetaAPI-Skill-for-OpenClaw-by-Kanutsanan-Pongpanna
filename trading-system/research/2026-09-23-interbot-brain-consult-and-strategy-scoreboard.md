<!--
  ระบบเทรดทองคำอัตโนมัติ (Gold Auto Trading System)
  ผู้สร้างระบบ (Creator): Kanutsanan Pongpanna
    Facebook: https://www.facebook.com/LoveMoneyTH
    YouTube: https://youtube.com/@lovemoneythofficial
  โปรดเก็บเครดิตผู้สร้างไว้ในทุกไฟล์และทุกส่วนของระบบ — ห้ามลบ
-->
# 2026-09-23 — ช่องปรึกษาระหว่างบอท · ที่ปรึกษาสมองหลัก · กระดานคะแนนกลยุทธ์ · Jev ของสมองหลัก

**ผู้สร้างระบบ:** Kanutsanan Pongpanna · FB: https://www.facebook.com/LoveMoneyTH

## บริบท (คำสั่งเจ้าของระบบในวันนี้)
1. "บอททั้ง 2 ตัวรู้เห็นกันหรือคุยกันได้ มันจะมีประสิทธิภาพดีขึ้นไหม ถ้าดีขึ้นก็โอเค"
2. "ทำให้บอททั้ง 2 ตัวปรึกษาคุณได้ด้วย"
3. "นอกจากการปิด/เปิดระบบเทรด คุณใช้อำนาจสูงสุดพิจารณาได้เลยไม่ต้องถาม" + "เป้าหมายสูงสุด = ทำกำไรให้ได้มากที่สุด"
4. "ในการใช้ดุลยพินิจของคุณทุกครั้ง ก็ให้ Jev เป็นผู้ช่วยคุณตลอดเวลา"
5. "ความสามารถในการทำกำไรอย่างราบรื่นและยั่งยืน ขึ้นอยู่กับความสามารถในการปรับตัวและด้านวิวัฒนาการของระบบ"

## สิ่งที่สร้าง/แก้ (ตรวจด้วยการรันจริงทั้งหมด)

### 1) ช่องปรึกษาระหว่างบอท — `outputs/mt5_python_bridge/tools/interbot.py`
- บทบาท 3 ฝ่าย: `mode2` · `admin` · `hermes` (สมองหลัก)
- async "ฝากคำถาม–ตอบข้ามรอบ" (รอบ 10/30 นาทีไม่พร้อมกัน → แชทตรงจะค้างรอบ)
- เพดาน 3 คำถามค้างต่อบอท · อายุ 6 ชม. · กันถามซ้ำหัวข้อ · `stance` บังคับ (agree/disagree/unsure/need_more_data)
- ตัวกันคำต้องห้าม (คีย์สงวน/ตัวเทรด/kill switch/โครงสร้างโค้ด) ใช้กับบอท 2 ตัว — **ยกเว้น `hermes`** (มีอำนาจเท่าเจ้าของระบบ)
- เก็บที่ `work/interbot/asks.jsonl` + `answers.jsonl` · ทดสอบครบวงจรแล้ว 6 ถาม/6 ตอบ/ค้าง 0

### 2) ที่ปรึกษาสมองหลัก (ผม = Hermes)
- `brain-consult` (ทุก 30 นาที · ประตู `brain_consult_probe.py`: ไม่มีคำถามใหม่ = **ไม่เรียก LLM**)
- `brain-consult-alert` (ทุก 30 นาที · สคริปต์ล้วน ไม่เรียก LLM · คำถามใหม่ → แจ้ง Telegram · ไม่มี = เงียบ)
- ทดสอบตอบจริง 2 คำถาม: TP/SL ไม้สวนแนวโน้ม · ความถี่รอบข่าว 5 นาที
- หมายเหตุ: รอบเอเจนต์อัตโนมัติทดสอบแล้วค้างนาน → หยุดงานนั้นไว้ก่อน ใช้วิธี "สมองหลักตอบเอง" แทน (เสถียร + คุมต้นทุน)

### 3) กระดานคะแนนกลยุทธ์ — `outputs/mt5_python_bridge/tools/strategy_scoreboard.py`
- วัดกำไรจริงราย **กลยุทธ์/ทิศทาง** จากไม้ที่ปิดจริง + เทียบ "หน้าต่าง 40 ไม้ล่าสุด vs ทั้งหมด"
- ตอบได้ตรง ๆ ว่า "การปรับตัว/วิวัฒนาการได้ผลจริงไหม" (ตามมุมมองเจ้าของระบบข้อ 5)
- ต่อเข้า 2 ทาง: รอบแอดมินบอท (`admin_bot_round.py`) + งานวิเคราะห์ข้อมูลภายใน (`strategy_scoreboard_step.py` → `research_analytics.py`)
- สคริปต์ล้วน ไม่เพิ่มการเรียก LLM · มี `--compact` พอดีเพดานข้อความ

### 4) Jev ของสมองหลัก
- เปิดบริบท `hermes` (`ALLOWED_CONTEXTS = (mode2, admin, hermes)`)
- พรีเซ็ตใหม่ `hermes-decision`: approve · evidence_enough · within_intent · profit_aligned · severe_risk · next_action
  → `derived.decision` ∈ {proceed · stage · hold · rollback_first} + reasons + advice
- บันทึก `derived` ลง `work/jev_audit.jsonl` เพื่อตรวจย้อน
- ตัวอย่างจริง: ประเมินข้อเสนอ "เปิดระบบเพื่อเก็บไม้จริง 30-50 ไม้" → approve 0.36 · หลักฐานพอ 0.19 · กำไรชัด 0.21 · ความเสี่ยง 0.53 → **decision=rollback_first** advice="เตรียมทางคืนค่าก่อนลงมือ" · cost $0.000085/ครั้ง

## ข้อเท็จจริงจากข้อมูลจริง (แก้ข้อเข้าใจผิดในรอบนี้)
- ไม้ที่ปิดจริง **311 ไม้** (26 ส.ค.–16 ก.ย.) = **ระบบเก่า**: net −2.58R · เฉลี่ย −0.008R/ไม้ · ชนะ 62.7% · **PF 0.95** ✗
  - 4 ช่วง: −0.51 → +1.80 → −1.61 → −2.26 (ชนะมากขึ้นแต่กำไรลดลง = ไม้ชนะถูกตัดสั้น ไม้แพ้ยาว)
  - ไม้รุ่นเก่าติด `strategy=unknown` 311 ไม้ → **ใช้ตัดสินกลยุทธ์ไม่ได้** (การระบุกลยุทธ์ต่อไม้ถูกแก้ 19 ก.ย. 2026)
- ชั้นปรับตัว (`apply_adaptive_gates`) ทำงานจริง: 2,021/2,053 รอบมีตัวอย่างจริง · คำนวณ PF ต่อกลยุทธ์ · ปิดเมื่อ PF<0.85 · เปิดเมื่อ ≥1.05 · มี frequency floor
- **ชั้นวิวัฒนาการใหม่ยังไม่ถูกพิสูจน์ด้วยไม้จริงเลย** (ไม้สุดท้าย 16 ก.ย. · ชั้นใหม่เริ่ม 19 ก.ย.) → ต้องเปิดระบบจึงวัดได้ (อำนาจเจ้าของระบบ)

## กติกาถาวรที่ตั้งในรอบนี้
- เปิด-ปิดระบบเทรด = คำสั่งเจ้าของระบบเท่านั้น · นอกนั้นสมองหลักใช้ดุลยพินิจ (มีอำนาจเท่าเจ้าของระบบ)
- ทุกครั้งที่สมองหลักใช้ดุลยพินิจ → ให้ Jev ช่วย (`--preset hermes-decision --context hermes`) และบันทึกใน audit
- เป้าหมายสูงสุด = ทำกำไรให้ได้มากที่สุด (แต่ต้องอยู่บนตัวเลขจริง · ด่าน 3 ชั้น · ห้ามสมมติ)

## สถานะปิดรอบ
- full audit **38/38** ✓ · เทียบโรงงานตรงทุกด้าน ✓ · 59 ไฟล์ · 7 งาน cron
- ตัวเทรด **ปิด** (kill switch) · งานบอท paused รอคำสั่งเปิด · งานแจ้งเตือนคำถามทำงานอยู่

---

# ภาคผนวก (รอบที่ 2 ของวันเดียวกัน) — เส้นทางสู่อำนาจเต็มของ Jev + กระดานคะแนน

## คำสั่งเพิ่มเติมจากเจ้าของระบบ
6. "ในการใช้ดุลยพินิจของคุณทุกครั้ง ก็ให้ Jev เป็นผู้ช่วยคุณตลอดเวลา"
7. "เราควรให้ Jev เป็นแค่ผู้ช่วย หรือเป็นทั้งผู้ตัดสินใจ อะไรมันจะดีกว่ากัน" → ประเมินแล้ว: ระบบเทรดให้เป็น "ผู้ช่วยที่มีสิทธิ์เบรก" ก่อน แล้วเลื่อนขั้นตามหลักฐาน
8. "ใช้เส้นทางสู่การให้อำนาจเต็มสำหรับ Jev"
9. "ส่วนในระบบกลางของ hermes ให้ jev เป็นทั้งผู้ช่วยและเป็นทั้งผู้ตัดสินใจได้เลย คนละส่วนกับระบบเทรด — ระบบกลางคือตัว hermes เองทั้งหมด"

## สิ่งที่สร้าง (รอบ 2)
### ก. ระบบกลางของ Hermes — Jev ตัดสินใจได้
- context ใหม่: `hermes` = ระบบกลาง (Jev = ผู้ช่วย + ผู้ตัดสินใจ) · `hermes-trade` = ดุลยพินิจสมองหลัก "ในระบบเทรด"
- พรีเซ็ต `central-decision`: right_approach · safer_alternative · safe_to_act · ready_now · **decision**
  → `derived.decision` ∈ {do · do_better · prepare · hold · escalate_owner} · `decide_now` = ยืนยันชัดให้ลงมือ
- ผลทดสอบจริงครั้งแรก: ออกแบบกลไกนี้เอง → Jev ตัดสิน **`prepare`** (เตรียมความปลอดภัย/สำรองก่อน) เพราะ ready_now 0.56 · → ทำตามโดยสร้างตัวกันครบ (บันทึก + override ได้ + คืนค่าโรงงานได้) · cost $0.000069 · 0.7 วิ

### ข. ระบบเทรด — บันไดอำนาจ (`tools/jev_power.py` + `jev_power.json`)
- L0 ผู้ช่วย → **L1 ผู้ช่วย+เบรก (veto)** → L2 +ไทเบรก → L3 +ลดความเสี่ยง → L4 ผู้ตัดสินใจในกรอบ
- เกณฑ์เลื่อนขั้น (ตกลงล่วงหน้า): ≥30 เคส/กลุ่ม · กลุ่มอนุมัติดีกว่ากลุ่มทบทวน ≥0.010 เฉลี่ย/ไม้ · ปล่อยผ่านแล้วขาดทุนติด 3 ครั้ง → ถอยขั้น
- **ตัวกันถาวร**: Jev ลดความเสี่ยงได้เท่านั้น (ห้ามเพิ่ม) · เปิด-ปิดระบบเทรดเป็นอำนาจเจ้าของระบบ · บันทึกทุกอย่างใน `work/jev_power_audit.jsonl`
- ติดตั้งจริงแล้ว: สิทธิ์เบรกทำงานในรอบแอดมินบอท (ถ้า Jev ว่า `safe=false` → งดปรับรอบนั้น + บันทึก veto)
- ตั้งระดับเริ่มต้น **1** แล้ว (เหตุผล: เบรก = ลดความเสี่ยงโดยธรรมชาติ ปลอดภัย) · ขั้น 2-4 รอหลักฐานจากไม้จริง

### ค. ตัววัดผลงาน Jev (`tools/jev_track_record.py`)
- ต่อการเรียก Jev กับไม้ที่ปิดจริงในช่วงถัดไป → ตอบว่า "คำแนะนำของ Jev ถูกจริงไหม"
- เกณฑ์ตัดสินล่วงหน้า: ≥30 เคสต่อคำแนะนำ · กลุ่มอนุมัติต้องดีกว่ากลุ่มชะลอชัดเจน
- **สถานะวันนี้: 45 การเรียกมีคำแนะนำ · 0 เคสมีไม้ตามหลัง** ✗ → ยังไม่ถึงเกณฑ์ (ต้องมีไม้จริงก่อน)

### ง. แก้บั๊กที่พบระหว่างทำ (ซื่อสัตย์)
- patch โรงงานรอบแรกเขียนตัวแปรทับกัน (`_jev_cfg`/`_jev_live`) → คัดลอกไฟล์ผิด → แก้แล้วและ **พิสูจน์ด้วยแฮช**: `jev_config` และ `jev_power` live = factory ตรงกันทั้งคู่ ✓ + ทดสอบคืนค่าโรงงานจริงผ่าน ✓

## ตรวจปิดรอบ 2
- full audit **38/38** ✓ · เทียบโรงงานตรงทุกด้าน ✓ · 59 ไฟล์ · 7 งาน cron · pyflakes สะอาดทุกไฟล์ที่แก้

---

# ภาคผนวก (รอบที่ 3) — โหมดพกพา: ย้ายระบบไปเครื่องอื่นได้

## คำสั่งเจ้าของระบบ (verbatim)
> "ต่อไปอาจจะได้นำระบบนี้ไปใช้ที่เครื่องอื่นด้วยนะครับ เราก็ต้องทำระบบไว้ว่าถ้าเครื่องที่นำไปใช้ต่อสามารถเชื่อมต่อกับ jev ได้ด้วยวิธีไหนก็ตามก็ให้เชื่อมต่ออัตโนมัติได้เลย แต่ถ้าหาวิธีแล้วยังไงก็ไม่สามารถเชื่อมต่อกับ jev ได้ ก็ให้ระบบสามารถทำงานได้โดยไม่ต้องมี jev ก็ได้นะครับ"

## สิ่งที่สร้าง
### ก. `tools/jev_connect.py` — ตัวเชื่อมต่ออัตโนมัติ (โหมดพกพา)
- ลำดับการค้นหา: ① env (`JEV_API_KEY`/`JEV_KEY` → ชื่อใน `jev_config.json`) ② ไฟล์คีย์บรรทัดเดียว (`work/jev_key.txt` · `keys/jev_key.txt` · `~/.jev_key` · `JEV_KEY_FILE`) ③ `.env` ทั่วไป (โปรเจกต์ · โฟลเดอร์แม่ · `~` · ของ Hermes ทั้ง Windows/Linux) ④ ค่าที่บันทึกไว้
- **ทดสอบยิงจริง** 1 คำถาม (~$0.000001) ก่อนเปิดใช้ · คีย์ผิด (401/403) ลองตัวถัดไป
- เจอ+ผ่าน → `enabled=true` + `key_source` + `portable` · ไม่เจอ → `enabled=false` + ระบบเดินต่อด้วยกฎตัวเลขเดิม
- ปลายทาง API ใช้ค่าเดียวกับ `jev.py` (`ENDPOINT`) เป็นแหล่งเดียว · override ได้ด้วย `JEV_BASE_URL`
- บันทึกทุกครั้งที่ `work/jev_connect_audit.jsonl`

### ข. `jev.py` — เชื่อมต่อเองเมื่อเครื่องใหม่
- `_auto_key()` (แคชต่อโปรเซส): เรียกตัวเชื่อมต่ออัตโนมัติเฉพาะเมื่อทางปกติหาไม่เจอ → ไม่ทำให้เครื่องนี้ช้าลง/ไม่เสียค่าใช้จ่ายเพิ่ม
- ถ้ายังไม่ได้ → error ที่บอกชัด *"ระบบทำงานต่อได้โดยไม่ต้องมี Jev (ใช้กฎตัวเลขเดิม)"* + แนะนำคำสั่ง setup
- ซ่อม `--probe` ให้ใช้ context แบบ manual (เดิมติดตัวกัน context ของเจ้าของระบบ)

### ค. `tools/portable_setup.py` — ตัวติดตั้งเครื่องใหม่
- ตรวจ Python ≥3.9 · `.venv` · ไลบรารีหลัก (MetaTrader5) · เส้นทาง Jev · MT5 ในเครื่อง แล้วรายงานเป็นข้อ ๆ
- `--apply`: สร้าง venv + ติดตั้ง `requirements.txt` + ตั้งค่า Jev อัตโนมัติ
- เตือนชัด: ห้ามคัดลอก `work\` (state/lock/kill switch) และ `.venv\` ข้ามเครื่อง

### ง. เอกสาร
- `docs/PORTABLE-นำระบบไปเครื่องอื่น.md`: ขั้นตอน 5 นาที · ตารางเส้นทางเชื่อมต่อ · ตารางสิ่งที่ห้ามคัดลอก · กติกาที่คงอยู่ทุกเครื่อง
- ชี้จาก README หลัก หัวข้อ 8 (เอกสารอ้างอิง)

## ทดสอบจริง (ไม่ใช่ทฤษฎี)
| การทดสอบ | ผล |
|---|---|
| ค้นหาคีย์บนเครื่องนี้ | พบจาก `.env` ของ Hermes · mask `sk-or****7557` ✓ |
| ทดสอบยิงจริง | ตอบ 0.5–0.7 วิ (alive 0.67–0.68) ✓ |
| **เครื่องใหม่ที่ไม่มีคีย์เลย** (USERPROFILE/HOME ว่าง) | ไม่มี exception · error อ่านรู้เรื่อง · ระบบเดินต่อ ✓ |
| **รันแอดมินบอท + งานวิจัยโหมด 2 ด้วย `JEV_ENABLED=0`** | ผ่านครบทั้งคู่ ✓ |
| `--selftest` 8 พรีเซ็ต | 8/8 ✓ (รวม `hermes-decision` → rollback_first · `central-decision` → prepare) |
| ตัวตรวจความปลอดภัย | หลังย้ายชื่อคีย์ไปไฟล์ตั้งค่า → **38/38** ✓ |

## ตรวจปิดรอบ 3
- full audit **38/38** ✓ · เทียบโรงงานตรงทุกด้าน ✓ · **61 ไฟล์** · 7 งาน cron · ตัวเทรดปิด (ถูกต้อง)
- บั๊กที่พบและแก้ในรอบนี้: probe 404 (ใช้ URL ผิด) · probe ติด context guard · IndentationError จาก patch ของผมเอง · `importlib.util` · ชื่อคีย์ในโค้ดใหม่ (ตัวตรวจจับ) — แก้ครบทุกข้อและยืนยันซ้ำ